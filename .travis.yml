language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -U pip
  - pip install --progress-bar off -r requirements.txt
# command to run tests
script:
  - make test_all

